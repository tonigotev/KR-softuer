services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data/
    environment:
      POSTGRES_DB: waypoint_db
      POSTGRES_USER: waypoint_user
      POSTGRES_PASSWORD: waypoint_password_2024
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U waypoint_user -d waypoint_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    volumes:
      - .:/home/app/
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    environment:
      SECRET_KEY: ${SECRET_KEY:-django-insecure-placeholder-key-change-in-production}
      ALLOWED_HOSTS: '*'
      USE_POSTGRES: "True"
      DB_NAME: waypoint_db
      DB_USER: waypoint_user
      DB_PASSWORD: waypoint_password_2024
      DB_HOST: db
      DB_PORT: 5432

volumes:
  pgdata:
